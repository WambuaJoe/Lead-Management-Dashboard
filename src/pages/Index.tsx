import { Layout } from '@/components/Layout';
import { LeadForm } from '@/components/LeadForm';

const Index = () => {
  return (
    <Layout>
      <LeadForm />
    </Layout>
  );
};

export default Index;
